{
  "mcpServers": {
    "zenborg": {
      "command": "pnpm",
      "args": [
        "--silent",
        "-C",
        "./mcp-server",
        "start",
        "--vault",
        "${HOME}/.zenborg"
      ]
    }
  }
}
