{
  "name": "zenborg-native-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx index.ts",
    "build": "bun build index.ts --target=node --outdir dist",
    "build:compile": "bun build index.ts --compile --target=bun --outfile dist/zenborg-native-host",
    "install:manifest": "bash install.sh"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
