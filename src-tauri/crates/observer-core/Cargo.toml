[package]
name = "observer-core"
version = "0.1.0"
edition = "2021"
description = "Desktop observer domain + writer — pure logic, no Tauri."

[dependencies]
chrono = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
